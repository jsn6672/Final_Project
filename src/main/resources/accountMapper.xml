<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sh.pj.account.AccountMapper">

	<select id="getUserID" resultType="com.sh.pj.account.AccountDTO">
		select * from jsn_user_tbl where user_id = #{user_id}
	</select>

</mapper>