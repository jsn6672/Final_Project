<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sh.pj.mypage.MypageMapper">

	<select id="getMember" resultType="com.sh.pj.account.MemberDTO">
		select * from jsn_user_tbl
		where user_id = #{user_id}
	</select>
	
	 <update id="PWUpdate" parameterType="com.sh.pj.account.MemberDTO">
        update jsn_user_tbl
        set user_pw =
        #{user_pw}
        where user_id = #{user_id}
    </update>

	 <update id="updateProfile" parameterType="com.sh.pj.account.MemberDTO">
        update jsn_user_tbl
        set user_location = #{user_location }, user_phone = #{user_phone}, user_pic = #{user_pic}
        where user_id = #{user_id}
    </update>
    
    <select id="getMemberById" resultType="com.sh.pj.account.MemberDTO">
    	select * from jsn_user_tbl
		where user_id = #{user_id}
    	
    </select>
    
    <select id="getAllmom" resultType="com.sh.pj.mom.MomDTO">
    	select * from momsitter_tbl where ms_id = #{ms_id}
    </select>
    

</mapper>